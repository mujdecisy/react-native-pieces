export enum ValidStates {
  VALID,
  INVALID,
  UNDEFINED,
}

export enum InputTypes {
  TEXT,
  NUMBER,
  DATE,
  SINGLE,
  MULTI,
}

export enum Settings {
  TEXT_MULTILINE_6,
  TEXT_MULTILINE_12
}
